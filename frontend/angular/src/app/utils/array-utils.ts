/*
interface Array {
  joinNonBlank: (separator: string) => string;
}

Array.prototype.joinNonBlank = function(separator: string): string {
  let nonBlankArray = this.filter((item) => {
    return typeof item === 'string' && item.length > 0;
  });
  return nonBlankArray.join(separator);
};
*/
